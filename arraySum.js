let numbrs = [50,10, 30, 50, 60, 40, 90, 70]
let sum = 0;
 
for (let i = 0; i < numbrs.length; i++) {
    let element = numbrs[i];
    sum = sum + element;
}
console.log("Total of the number is: ", sum);